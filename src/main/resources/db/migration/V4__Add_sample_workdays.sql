-- Add sample working days for testing

-- Add working days for the next 30 days with default time slots
INSERT INTO work_days (date, time_slots, is_working)
SELECT 
    date('now', '+' || value || ' days'),
    '["10:00","11:00","12:00","13:00","14:00","15:00","16:00","17:00","18:00"]',
    1
FROM (
    SELECT 0 AS value UNION SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION 
    SELECT 5 UNION SELECT 6 UNION SELECT 7 UNION SELECT 8 UNION SELECT 9 UNION
    SELECT 10 UNION SELECT 11 UNION SELECT 12 UNION SELECT 13 UNION SELECT 14 UNION
    SELECT 15 UNION SELECT 16 UNION SELECT 17 UNION SELECT 18 UNION SELECT 19 UNION
    SELECT 20 UNION SELECT 21 UNION SELECT 22 UNION SELECT 23 UNION SELECT 24 UNION
    SELECT 25 UNION SELECT 26 UNION SELECT 27 UNION SELECT 28 UNION SELECT 29
)
WHERE NOT EXISTS (SELECT 1 FROM work_days WHERE date = date('now', '+' || value || ' days'));
